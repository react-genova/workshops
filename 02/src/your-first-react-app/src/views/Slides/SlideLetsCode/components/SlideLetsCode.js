//@flow
import React from 'react';
import { Container, Important } from '../../Slide';

const SlideLetsCode = () => (
    <Container style={{justifyContent: 'center', fontSize: '130px'}}>
        <span>Less talk</span>
        <span>and</span>
        <span>more <Important>coding</Important></span>
    </Container>
);

export default SlideLetsCode;